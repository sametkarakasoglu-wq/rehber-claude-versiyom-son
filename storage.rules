rules_version = "2";
service firebase.storage {
  match /b/{bucket}/o {
    match /documents/{allPaths=**} {
      // ✅ Development: Herkese açık (TEST İÇİN)
      // ⚠️ Production'da daha güvenli rules kullan!
      allow read, write: if true;
    }
  }
}