rules_version = "2";
service firebase.storage {
  match /b/{bucket}/o {
    match /documents/{allPaths=**} {
      // Geliştirme ortamı için geçici olarak açık erişim
      // ÜRETİME GEÇİŞTE AUTH EKLENMELİ: if request.auth != null;
      allow read, write: if true;
    }
  }
}